---
title: DaySurface
description: An MCP server for Gmail - triage a ranked inbox, draft replies in a real composer, and fill and sign PDF attachments from any MCP client.
---

import { Inbox, PenLine, FileSignature, Terminal, Bot, Globe, Rocket, Ship, Webhook } from 'lucide-react';

**Triage, draft, sign - without leaving chat.** DaySurface puts a ranked inbox
and a real email composer inside Claude, ChatGPT, or whichever agent you use.
Your agent triages and writes; you edit and send.

It connects over streamable HTTP, so there is nothing to install - you add a
URL to your client and grant Gmail access once.

<Cards>
  <Card
    icon={<Rocket />}
    title="Quick Start"
    href="/docs/quickstart"
    description="Connect your client and run your first triage in under a minute."
  />
</Cards>

## What it does

<Cards>
  <Card
    icon={<Inbox />}
    title="Ranked inbox triage"
    href="/docs/mcp/tools"
    description="Rank recent threads by what needs attention, then work the list. Ranking is deterministic code, not a model."
  />
  <Card
    icon={<PenLine />}
    title="Drafts you edit, not approve"
    href="/docs/mcp/tools"
    description="Replies land in a real Gmail draft in an interactive composer. Your agent writes it; you press send."
  />
  <Card
    icon={<FileSignature />}
    title="Fill and sign PDFs in-thread"
    href="/docs/mcp/pdf-forms"
    description="Fill a PDF attachment's form fields and hand it back for signing - a step only you can complete."
  />
  <Card
    icon={<Webhook />}
    title="Webhook setup"
    href="/docs/gmail-webhooks"
    description="Configure Pub/Sub push, subscribe an HTTPS endpoint, and verify the signed events it receives."
  />
</Cards>

31 tools in total, all through the official Gmail API. See the full
[tool reference](/docs/mcp/tools).

## Connect it

<Cards>
  <Card
    icon={<Bot />}
    title="MCP Server"
    href="/docs/mcp"
    description="Add the server to Claude, ChatGPT, Cursor, VS Code, Goose, or any MCP client."
  />
  <Card
    icon={<Globe />}
    title="REST API"
    href="/docs/api"
    description="Call the same tools as authenticated HTTP endpoints from your own code."
  />
  <Card
    icon={<Terminal />}
    title="CLI"
    href="/docs/cli"
    description="Operate a self-hosted deployment from your terminal with the daysurface binary."
  />
</Cards>

Every tool is one pure function in a shared registry exposed over all three
transports, so behavior never drifts between them.

## Run it yourself

<Cards>
  <Card
    icon={<Ship />}
    title="Deployment"
    href="/docs/deployment"
    description="Self-host the full server on your own infrastructure with your own Google OAuth credentials."
  />
</Cards>

## Privacy

Your email is never sent to an AI model by us. DaySurface runs no LLM inference
on Gmail content, and the inbox ranking is deterministic code. Mail goes only to
the AI client you connected, under that client's own terms. Access uses Google's
`gmail.modify` scope - it can read, label, draft, and send, but it cannot delete
your mail permanently.

## Licensing & pricing

DaySurface is open source under the
[MIT license](https://github.com/Miyamura80/DaySurface/blob/main/LICENSE) - free
to use, modify, and self-host forever, with **no setup fee and no license cost**.

If you would rather not run it yourself, there are paid managed tiers, and
governance features (SSO, audit logs) are sold as an add-on you attach when you
need them. Tiers and current prices live on the
[pricing page](https://daysurface.com/pricing/), with a machine-readable manifest
at `/pricing.md` for agents comparing options.
